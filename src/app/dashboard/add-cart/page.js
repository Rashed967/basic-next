import React from 'react';

export const metadata = {
    title: 'add-cart | Next Hero',
    description: 'Next Hero',
  }

const AddCartPage = () => {
    return (
        <div>
            <h1>Add Cart page</h1>
        </div>
    );
};

export default AddCartPage;